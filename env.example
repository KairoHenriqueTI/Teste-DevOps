# Example environment file for production
# Copy this to .env on the EC2 instance and fill values (DO NOT commit .env)

DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_DATABASE=testdb
DB_USER=adminassaa
DB_PASSWORD=change_this_secure_password
